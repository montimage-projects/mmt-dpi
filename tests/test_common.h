/*
 * test_common.h — common includes for all test suites.
 * Provides system types (u_char, etc.) that the MMT headers expect
 * but don't always pull in themselves.
 */
#ifndef MMT_TEST_COMMON_H
#define MMT_TEST_COMMON_H

#include <sys/types.h>

#endif /* MMT_TEST_COMMON_H */
