# ThreadSanitizer suppressions for the multi-threaded harness (issue #65).
#
# Scope: this file may suppress ONLY known-benign or third-party reports that are
# NOT part of the Phase 6 thread-safety code under test. A race reported in
#   - src/mmt_core/src/packet_processing.c   (configured_protocols_mutex, #22)
#   - src/mmt_core/src/plugins_engine.c      (plugin_handlers_list_mutex, #22)
#   - src/mmt_tcpip/lib/protocols/proto_radius.c (per-session state, #23)
# is a REAL finding and MUST surface — never suppress those here.
#
# Suppression syntax (see the ThreadSanitizer docs):
#   race:<function-or-file-substring>
#   called_from_lib:<library-name>
#   deadlock:<function-substring>
#
# No suppressions are currently required: the harness runs clean against the
# BUILD=tsan SDK. Add an entry here only for a report you actually observe that
# is genuinely third-party/benign (e.g. a one-time-init race in a system
# library), with a comment explaining why it is safe to ignore.
