# Vendored third-party sources (issue #249, F-CLEAN-019).
#
# Files listed here are verbatim copies of upstream projects — they are
# excluded from the clean-code metrics (function-shape report, marker
# ratchet, commented-code lint, cppcheck scan, init-wrapper count) by this
# configuration, not by the convention of simply never naming them. Each
# entry carries its pinned upstream reference; a vendored file must also
# carry a VENDORED banner naming the same pin.
#
# Format: one repo-relative path per line; blank lines and #-comments are
# ignored. Consumers: tools/ci/report-function-shape.sh, lint-markers.sh,
# lint-commented-code.sh, lint-packet-path-io.sh, count-init-wrappers.sh,
# run-cppcheck.sh and scripts/validate-doc-drift.sh.
#
# nodejs/llhttp — pinned at tag release/v9.4.3 (generated release sources;
# adopted per docs/llhttp-migration.md, issue #222, replacing the archived
# nodejs/http-parser v2.9.4):
#   https://github.com/nodejs/llhttp/tree/release/v9.4.3
#   upstream include/llhttp.h  -> src/mmt_tcpip/lib/llhttp.h
#   upstream src/llhttp.c      -> src/mmt_tcpip/lib/llhttp.c
#   upstream src/api.c         -> src/mmt_tcpip/lib/llhttp_api.c
#   upstream src/http.c        -> src/mmt_tcpip/lib/llhttp_http.c
#   version macros: src/mmt_tcpip/lib/llhttp.h (LLHTTP_VERSION_*)
src/mmt_tcpip/lib/llhttp.h
src/mmt_tcpip/lib/llhttp.c
src/mmt_tcpip/lib/llhttp_api.c
src/mmt_tcpip/lib/llhttp_http.c
