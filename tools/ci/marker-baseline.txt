# Ratchet baseline for tools/ci/lint-markers.sh (issues #190/#232).
#
# Number of TODO/FIXME/XXX/HACK marker lines in tracked C/C++ sources outside
# the generated src/mmt_mobile/asn1c/ tree and the vendored sources listed in
# tools/ci/vendor-paths.txt. Task 5.8 triages the backlog — lower this number
# as it lands. The gate fails only when the count RISES above this value.
#
# #249: re-baselined 199 -> 188 — the vendored http_parser.{c,h} exclusion
# (F-CLEAN-019) removed 2 markers and the value also absorbs 9 markers
# burned down since the gate was introduced.
#
# #232: re-baselined 188 -> 170 — Task 5.8 triage: every surviving marker now
# carries an issue reference (see lint-markers.sh --strict, run blocking in
# CI). Word-boundary matching dropped 3 false positives (mkstemp "XXXXXX"
# templates, a "HACKED" test string); 10 stale/noise markers were deleted and
# 2 embedded in now-deleted commented-out code went away with them.
#
# #238: re-baselined 170 -> 161 — the nine `//BW: TODO(#330)` trailing comments
# on the inter-protocol registration calls moved verbatim from
# configured_protocols.c into inter_proto_classif_list.def, which the marker
# scan (tracked *.[ch] only) does not read; the markers still live there.
#
# #326: re-baselined 161 -> 107 — all 52 `TODO(#326)`/`XXX(#326)` markers in
# src/mmt_security were resolved: supported converter/operator branches were
# implemented (IPv6, float, timeval, string-pointer, generic header line,
# count windows, REPEAT) and intentionally unsupported data types were
# consolidated into marker-free fall-through cases; the remaining drop is
# dead commentary removed with them.
#
# #391: re-baselined 107 -> 80 — the cleanup-ratchet validation after the
# coverage/accuracy tests (#388, #390) found the count already at 80: the
# error-propagation work in #327 resolved 26 markers and #380 one more, but
# neither tightened this file. Every surviving marker is still issue-linked.
#
# #336: re-baselined 80 -> 76 — the three `TODO(#336)` placeholder-metric-ID
# markers in the fuzz engine's built-in VoIP model became named constants
# (VOIP_METRIC_ID_*), and the commented-out `kpi_value >= 0` range check in
# trapezoid_left() was answered (no lower bound, by design) and removed.
#
# #328: re-baselined 76 -> 72 — the four report/dump stubs in
# src/mmt_core/src/packet_processing.c that emitted a literal "TODO"
# (mmt_stats_snprintf/fprintf/format and the mmt_attr_snprintf default arm)
# now render the protocol statistics or return a documented negative code.
#
# #329: re-baselined 72 -> 70 — the two `TODO(#329)` "Throughout metrics"
# markers in src/mmt_core/src/packet_stats.c were resolved by a decision to
# keep cumulative per-packet protocol counters polled by the application
# (docs/DECISIONS.md, 2026-09-24).
#
# #332: re-baselined 70 -> 64 — the six INT/INT-report markers were resolved:
# IPv6 carriers and inner packets are parsed, the payload after INT stays
# PROTO_UNKNOWN for the QUIC_IETF-over-INT checker to reclassify, the stale
# 64-hop cap note and the LV2 egress-port gap (both layouts decoded) went away.
#
# #333: re-baselined 64 -> 58 — the six `TODO/FIXME(#333)` markers in
# proto_quic_ietf.{c,h} were resolved: QUIC v2 (RFC 9369) support, the
# header-protected reserved bits explained, session init/analysis documented
# at the classifier, coalesced QUIC-after-QUIC classification, a session
# lookup for chained layers and the short-header DCID length learned from
# the flow's long headers.
58
